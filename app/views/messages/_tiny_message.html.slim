

.tiny-message
  .tiny-message-header
    = image_tag "users/#{message.user.image}", class: "tiny-user-image"
    .tiny-message-username = message.user.name
  .message-text = process_message message.text
  .message-time = message.created_at.strftime("%b #{message.created_at.day.ordinalize} at %-I:%M%p")
